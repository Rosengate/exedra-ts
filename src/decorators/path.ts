import {Decorator} from "./decorator.js";

export const path = (path: string) : MethodDecorator => {
    return (target, propertyKey, descriptor) => {

    }

// export default function path(path: string) : any {
//     return () => {
//     }
}